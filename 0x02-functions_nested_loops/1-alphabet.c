#include "main.h"
#include <stdio.h>
/**
 * prints the alphabet in lowercase
 * followed by a new line
 */

void print_alphabet(void)
{
char alphabet;

for (alphabet = 'a'; alphabet <= 'z'; alphabett++)
{
	_putchar(alphabet);
}
_putchar('\n');
}
