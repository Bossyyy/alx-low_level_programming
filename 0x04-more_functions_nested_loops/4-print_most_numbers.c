#include "main.h"

/**
 * print_numbers - print 0-9
 * return: void no 2 and 4
 */

void print_numbers(void)
{
        char a;

        for (a = '0'; a <= '9'; a++)
        {
                if (!(c == '2' || c == '4'))
			_putchar(c);
        }
        _putchar('\n');
}
